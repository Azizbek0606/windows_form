<Window x:Class="data_binding.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:data_binding"
        mc:Ignorable="d" ResizeMode="NoResize"
        AllowsTransparency="True" Height="250" Width="300" WindowStyle="None" WindowStartupLocation="CenterScreen" Background="Transparent">
    <Border CornerRadius="20" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBox Name="txtinput"
                     VerticalAlignment="Center"
                     VerticalContentAlignment="Center"
                     Width="250"
                     Height="40"
                     FontSize="16"
                     Text="{Binding BoundText , UpdateSourceTrigger=PropertyChanged, Mode=OneWayToSource}"/>
            <Button Grid.Row="1"
                    Name="btnSet"
                    Width="100"
                    Height="40"
                    Content="Set"
                    Click="btnSet_Click"/>
            <TextBlock Grid.Row="2"
                       Name="tbResult"
                       FontSize="16"
                       Text="{Binding BoundText}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</Window>
